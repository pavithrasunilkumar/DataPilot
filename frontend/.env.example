# URL of the DataPilot backend API.
# For local development with docker-compose or `uvicorn`, this is correct as-is.
# For a Vercel deployment, set this in Vercel's Project Settings -> Environment
# Variables to your deployed backend URL (e.g. https://datapilot-api.onrender.com).
VITE_API_URL=http://localhost:8000
